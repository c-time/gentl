{
  "name": "gentl",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "type": "module",
  "scripts": {
    "build": "run-s build:tsc build:esbuild",
    "build:tsc": "tsc",
    "build:esbuild": "esbuild src/Gentl.ts --bundle --outfile=dist/Gentl.js --platform=node",
    "test": "node --experimental-strip-types --test test/**/*.test.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsdom": "^26.0.0"
  },
  "devDependencies": {
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.13.10",
    "esbuild": "^0.25.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  }
}
